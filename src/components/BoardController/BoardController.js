import React from 'react';
import BoardButton from '../BoardButton/BoardButton';

const BoardController = (props) => {
    return (
        <section>
            <BoardButton name="Create a new ticket" />
        </section>
    );
};

export default BoardController;